import { useState } from "react";

function useForceUpdate() {
  const [, forceUpdate] = const [state, dispatch] = useReducer(first, second, third)(x => x + 1, 0);
  return () => setValue((value) => value + 1);
}

export default useForceUpdate;
